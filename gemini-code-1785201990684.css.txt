* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
  user-select: none;
  -webkit-user-select: none;
}

body {
  background-color: #232b28;
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  display: flex;
  justify-content: center;
  align-items: center;
  width: 100vw;
  height: 100vh;
  overflow: hidden;
}

#game-container {
  position: relative;
  width: 100%;
  max-width: 800px;
  height: 100%;
  max-height: 600px;
  box-shadow: 0 12px 28px rgba(0,0,0,0.6);
  border-radius: 10px;
  overflow: hidden;
  border: 4px solid #4a3525;
  background: #5b8c38;
}

canvas {
  display: block;
  image-rendering: pixelated;
  image-rendering: crisp-edges;
}

/* Touch Controls */
#touch-controls {
  position: absolute;
  bottom: 12px;
  left: 12px;
  right: 12px;
  display: flex;
  justify-content: space-between;
  align-items: flex-end;
  z-index: 5;
  pointer-events: none;
}

.d-pad {
  display: grid;
  grid-template-columns: repeat(3, 44px);
  grid-template-rows: repeat(3, 44px);
  gap: 4px;
  pointer-events: auto;
}

.btn-ctrl {
  background: rgba(35, 43, 40, 0.85);
  border: 2px solid #f2e3c6;
  border-radius: 8px;
  color: #f2e3c6;
  font-size: 1.1rem;
  font-weight: bold;
  display: flex;
  justify-content: center;
  align-items: center;
  touch-action: manipulation;
  cursor: pointer;
}

.btn-ctrl:active {
  background: rgba(139, 80, 35, 0.9);
}

.btn-up { grid-column: 2; grid-row: 1; }
.btn-left { grid-column: 1; grid-row: 2; }
.btn-down { grid-column: 2; grid-row: 2; }
.btn-right { grid-column: 3; grid-row: 2; }

.hint-tag {
  background: rgba(35, 43, 40, 0.85);
  color: #f2e3c6;
  padding: 6px 14px;
  border-radius: 16px;
  font-size: 0.8rem;
  border: 1px solid #7c5737;
  pointer-events: auto;
}

/* Modal Popup */
.modal {
  display: none;
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 85%;
  max-width: 440px;
  background: #fdf6e3;
  border: 4px solid #7c5737;
  border-radius: 12px;
  padding: 20px;
  box-shadow: 0 10px 25px rgba(0,0,0,0.5);
  z-index: 10;
  color: #3a2e2b;
}

.modal.active {
  display: block;
  animation: popIn 0.2s ease-out;
}

@keyframes popIn {
  from { transform: translate(-50%, -45%) scale(0.95); opacity: 0; }
  to { transform: translate(-50%, -50%) scale(1); opacity: 1; }
}

.modal h2 {
  color: #2b573a;
  font-size: 1.3rem;
  margin-bottom: 4px;
  border-bottom: 2px dashed #b8860b;
  padding-bottom: 4px;
}

.modal .category {
  display: inline-block;
  background: #e2ebd0;
  color: #2b573a;
  font-weight: bold;
  font-size: 0.75rem;
  padding: 3px 8px;
  border-radius: 10px;
  margin-bottom: 10px;
}

.modal p {
  line-height: 1.4;
  font-size: 0.9rem;
  margin-bottom: 14px;
  user-select: text;
  -webkit-user-select: text;
}

.modal .contact-btn {
  display: inline-block;
  background-color: #3b7a57;
  color: #ffffff;
  text-decoration: none;
  padding: 8px 14px;
  border-radius: 6px;
  font-weight: bold;
  font-size: 0.85rem;
}

.modal .close-btn {
  position: absolute;
  top: 8px;
  right: 12px;
  background: none;
  border: none;
  font-size: 1.5rem;
  font-weight: bold;
  color: #7c5737;
  cursor: pointer;
}